[build]
builder = "dockerfile"

[deploy]
startCommand = "dotnet RentalOS.API.dll"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
 Eskom Railway deployment configuration successful. Eskom automated health monitoring.
